# `@katsutosh/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ npm install --save-dev @katsutosh/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@katsutosh/prettier-config"
}
```
